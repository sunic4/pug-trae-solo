---
doc_type: refactor-scan
refactor: 2026-04-25-refactor-loop-14
status: draft
scope: computed.ts 文件
---

# computed.ts 文件重构扫描

## 1. 代码结构概览

- **文件路径**: `packages/reactivity/src/computed.ts`
- **主要功能**: 实现计算信号系统，包括计算信号的创建、订阅和依赖跟踪
- **主要组件**:
  - `ComputedSignal` 接口：定义计算信号的基本操作
  - `computed` 函数：创建计算信号实例
  - 内部函数 `recompute`：重新计算值
  - 内部函数 `markDirty`：标记计算信号为脏

## 2. 重构机会

### 2.1 优化 computed 函数

- **问题**: `computed` 函数缺少对 `fn` 参数的类型检查
- **建议**: 添加对 `fn` 参数的类型检查，确保传入的参数是函数
- **影响范围**: 低
- **风险**: 低

### 2.2 优化 ComputedSignal 接口

- **问题**: `ComputedSignal` 接口缺少辅助方法，如 `clearSubscribers` 和 `subscriberCount`
- **建议**: 添加这些辅助方法，与 `Signal` 接口保持一致
- **影响范围**: 低
- **风险**: 低

### 2.3 优化错误处理

- **问题**: 虽然已经有错误处理，但可以进一步优化
- **建议**: 添加更多的错误处理，提高代码的健壮性
- **影响范围**: 低
- **风险**: 低

### 2.4 优化代码结构

- **问题**: 代码结构可以更清晰，注释可以更详细
- **建议**: 添加更多的注释，优化代码风格和可读性
- **影响范围**: 低
- **风险**: 低

## 3. 重构建议

1. **优化 computed 函数**：添加对 `fn` 参数的类型检查
2. **优化 ComputedSignal 接口**：添加 `clearSubscribers` 和 `subscriberCount` 方法
3. **优化错误处理**：添加更多的错误处理
4. **优化代码结构**：添加更多的注释，优化代码风格和可读性

## 4. 重构计划

1. 优化 `computed` 函数，添加对 `fn` 参数的类型检查
2. 优化 `ComputedSignal` 接口，添加 `clearSubscribers` 和 `subscriberCount` 方法
3. 优化错误处理，添加更多的错误处理
4. 优化代码结构，添加更多的注释

## 5. 风险评估

- **高风险**：无
- **中风险**：无
- **低风险**：所有重构点都是低风险的，不会破坏现有功能

## 6. 测试建议

- 运行现有的测试套件，确保重构不会破坏现有功能
- 可以添加新的测试用例来测试新添加的功能
