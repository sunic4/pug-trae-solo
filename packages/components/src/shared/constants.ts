import { Modifier } from '@pug-canvas-ui/layout'
import type { ReadonlyModifier } from '@pug-canvas-ui/layout'

const DEFAULT_MODIFIER: ReadonlyModifier = Modifier.create().freeze()

export { DEFAULT_MODIFIER }
